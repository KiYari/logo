import headerStyles from './header.module.css';
import {Typography} from "antd";

const Header = () => {
    return(<header className={headerStyles.head}>
        <p className={headerStyles.logo}>Logo</p>
        <hr/>
        cba
        </header>)
}

export default Header;